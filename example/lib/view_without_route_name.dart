import 'package:flutter/material.dart';

class NoRouteNamePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("不设置 Route Name"),
      ),
    );
  }
}
